# live-transcript scratch (_live/ used to hold these; tactiq_watch.py still writes here if re-created)
_live/
# runtime locks / state
.claude/scheduled_tasks.lock
# drive shortcuts (engagement file mirrors, regenerated locally)
**/_drive
# work-in-progress folders not yet placed in _internal/
Neuralith Design System/
# credentials, ever
*.env
*token*
*credentials*
# os / editor cruft
.DS_Store
*~
~$*
.idea/
.vscode/
# python
__pycache__/
*.pyc
