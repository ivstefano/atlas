# atlas — current state (read this first)

This is the ops layer for Ivo's solutions-architect work: the plan, the agent map, the loop
specs, and (eventually) the tooling. It is **not** a behavioral clone of Ivo — it does the
mechanical parts so the judgment stays with him.

Two sibling repos under `~/Documents/`:
- **`atlas/`** (this one) — the plans/specs/tooling. Read order: this file → `ENGAGEMENT_PLAN.md` → `README.md`.
- **`engagements/`** — one folder per client engagement, same shape every time. See its `README.md`.
- (`Axion/` still exists — the old junk drawer: `*-usecase/` reference dirs, `tactiq_export/`, playground demos, loose files. A separate cleanup, not blocking anything. `tactiq_export/export.py` is still where the live-transcript fetch reaches.)

## What's been done (as of 2026-05-13)

1. **The agent map** — `AGENT_MAP.html` (v1) and `AGENT_MAP_V2.html` (cleaner redesign: trimmed cards, 3-col grid, impact/effort badges, footnotes section). Built live during Strategy Day Part 2 from the Tactiq transcript. Source transcript: `Strategy Day - Part 2 - AI Driven Development Knowledge Sharing.txt`. **Do not produce PNG/PDF exports** (Ivo's instruction); HTML only.
2. **`ENGAGEMENT_PLAN.md`** — the canonical plan for the engagement-context system. §0 has the decisions; §6 has the two-track build order (A = context system, B = tooling/infra); §7 has the 6 still-open questions; §8 has the AWS/sync architecture.
3. **`engagements/` repo created** (`~/Documents/engagements/`, git-initialised): `_template/` (the four core files + `1_pre-scoping/`), `/new-engagement <company>` command in `.claude/commands/`, `README.md`, `.gitignore` (keeps client data + credentials out: `*.pdf *.zip *.xlsx *.docx *.pptx`, `**/data/* **/raw/ **/processed/ **/results/`, `*credentials* *token* .env *.pem`, `.DS_Store`).
   - **Decided structure:** `engagements/<company>/` with `CONTEXT.md` + `STATUS.md` + `SOURCES.md` + `COMMERCIAL.md` at the top; numbered stage folders `1_pre-scoping / 2_scoping / 3_contracting / 4_poc / 5_implementation`, created lazily; `_legacy/` for old flat files; `_briefs/` for auto-briefs (committed); `_pending/` for trigger-staged new engagements awaiting Ivo's promotion.
4. **`atlas/` moved** out of `Axion/_optimize/` to `~/Documents/atlas/` and made its own git repo. Breadcrumb left at `Axion/_optimize/MOVED.md`.
5. **A3 — Heineken migrated** (`engagements/` commit `6c6c1f0`). `Axion/scoping/heineken/` (flat folder, ~20 transcripts + workbooks + an abandoned `_rag/` exploration) folded into `engagements/heineken/`: clean `CONTEXT/STATUS/SOURCES/COMMERCIAL` written from the transcripts + `BRIEFING.md` + the authored datastore-context doc; old `.md` files → `_legacy/`; transcripts split into `1_pre-scoping/transcripts/` (Feb–Mar use-case deep dives) and `4_poc/transcripts/`; the authored datastore-context (`human-in-the-loop/`) → `4_poc/datastore-context/`; the signed contract PDF → `3_contracting/contracts/`; the `_rag/` *code* → `_legacy/_rag-exploration/`. **Decisions made on the spot** (Ivo declined the upfront question, take as defaults): COPIED not moved (Axion source untouched, reversible — cleanup later); big binaries (workbooks, `heineken-rag-full-extract.zip`, `_rag/chroma_db`, `_rag/extracted/` ~175 files) **left in Axion**, `SOURCES.md` references the `AXION:` paths + future S3; no separate `2_scoping/` stage (went straight from deep dives → contracting → PoC); `5_implementation/` kept as a placeholder (Phase-2 actively being scoped). The `.gitignore` excludes the contract PDF + alignment/glossary `.docx` + `Heineken S3 answers.xlsx` — they sit in the working tree, referenced by path, *not committed* (consistent with "binaries don't go in git"; revisit if Ivo wants the contract tracked). Heineken facts now in `CONTEXT.md`: RAG-type Excel→DB+SQL-agent PoC over the D&T Cost&Value financial files; business owner **Justyna Roczek** (= Justyna Bień, married — both names appear in transcripts); manager/decision-maker **Lucy Todorovska**; procurement SME **Monika Samolej**; ~160 IT services across 4-5 workbooks; L-code is the universal join key; MAIN "In progress" sheet (rows 3-166) is source of truth; 6 scenarios built easiest-first S3→S1→S5→S4→S6→S2; human-in-the-loop demo (email draft, send mocked) + the open "continuous learning loop" question with Lucy; 8-week PoC from the Mar 23 contract HNK282127, ~ending mid/late May; post-PoC commercial+technical meeting + Phase-2 proposal pending.

6. **A4 — 7 more engagements migrated** (`engagements/` commit `fc2f4e3`, built by 7 parallel subagents per `atlas/_migration_protocol.md` — that protocol doc + `atlas/_iris_people.md` are reusable for the next batch). Same pattern as Heineken: four core files at the top, stage folders for stages reached, old flat files in `_legacy/`, transcripts split by stage, extractor code copied, **all client data / OCR-extraction output / multi-MB HTML showcases left in Axion** with `AXION:` paths in `SOURCES.md`, credentials never migrated, nothing binary committed (`.gitignore` catches `*.pdf *.xlsx *.docx *.pptx *.csv *.zip` etc.). What got migrated, with stage / type / key people / open Qs the subagents flagged:
   - **aumovio** — `axion-extraction`, `poc-implementation` (running long; ~2wk extension, blockers = Phil's availability + slow MOVEit relay + the 11-doc sample was unrepresentative). Aumovio SE (carve-out of Continental Safety & Motion): extract contracted parts-volume data from ~100 OEM contracts (multilingual PDF, ~9 Chinese) into a template → SAP. SMEs **Phil** (Strategy/ex-Ford — acceptance authority, hand-filled the 11-doc ground truth) + **Norbert** (AI&Data — champion, approved ~€60k full-impl budget); **Chen** (COPES procurement). Ivo took over from Michael Sica-Lieber ~Mar 2026. Eval mid-May: 11-doc sample ~71-81% (~88% F1 on cleaner subset); full ~91-doc run weak (~50% of priority cols, ~28% hallucination) → V6 baseline + hallucination post-processor; deeper preprocessing = "implementation, not PoC". Won't hit the SoW's ~92% precision metric — play is "directional + credible path to full impl". **Open:** exact PoC/full-impl contract figures (POC.md says €17.6k PoC / ~€60k full / ~€90k total — not in folder, in signed contract/HubSpot); the Commercial Proposal + PO docs aren't in the folder; the ultimate business-line budget owner for the rollout is unnamed (Jordan flagged the gap). No `1_pre-scoping`/`2_scoping` — NDA + scoping pre-date the folder (2025), went straight SoW→PoC.
   - **sms-group** — `hybrid`, `poc-implementation` (wrapping up — the extreme hybrid case in ENGAGEMENT_PLAN §2). ~12wk PoC kicked off Feb 26 2026, €27.5k "at cost": unified structured digital representation of one machine, the **Side Trimmer** (Seitenschere), from cross-discipline docs — functional Excel "MuK list", mech/hydraulic/pneumatic BOMs, ~280MB / ~7,500-page EPLAN electrical PDF, Word functional descriptions, Creo 3D-model XML — under a Claude-Code-style agent with tree-view UI + NL query layer (in-house IRIS model on IRIS AWS) + "locate device on PDF". SME/champion **Thorsten Müller**; decision-maker **Gerald Mayr** (EVP — *leaves SMS autumn 2026*) + **Matthias**. "Done" = ~2hr results presentation + live demo to Gerald, targeted ~Tue 19 May 2026, then a staged "digital twin of a plant" full-impl proposal (reference-quote arrangement Jordan is driving). **Open:** SMS-side business + exec stakeholders never confirmed on file (Thorsten was to identify both); SMS PO number/value not in folder; the 19 May slot not booked yet; pending Thorsten items in `4_poc/POC_WRAPPING.md` (function-text column in EPLAN PDF? which device for locate-on-PDF demo? PLC address format `Q7001.2` / `%Q7001.2` / `A7001.2`?); some Dec-2025 scoping docs carry "2024" dates in their headers (treated as typos). `5_implementation/` is a stub. Note: "Vankata" appears both as IRIS's Ivan Tsenov *and* (in some notes) as a different person — disambiguate.
   - **postbank** (= **Eurobank Bulgaria**) — `rag` (Neuralith data-unification / semantic-layer + analytics-agent flavour), `scoping`. Wants an auto-built, *inspectable* semantic layer over their commercial team's Salesforce Marketing Cloud data + an analytics agent on top, two output modes (business-user reports w/ graph+table+explanation+recommendation+"numbers verified" line; Campaign-Ops ready-to-run SFMC-SQL). Evaluating Solid AI (getsolid.ai) in parallel. Relationship hook: champion **Svetla Yankova** went to high school with IRIS CTO Viktor Botev. A scoping demo *was* delivered — in-person at Postbank HQ Sofia 28 Apr 2026 on a synthetic SFMC dataset on the Neuralith unified-demo app (Postbank reversed sharing real data on 27 Apr, want an NDA first). Genuinely early: no NDA, no PoC contract, no real-data access. **Open:** outcome of the 28 Apr F2F is in no migrated file (did it land? NDA agreed? gated-PoC route?); no notes for the 22 Apr prep call; `SCOPING.md` ≈ `SCOPING_DEMO.md` (near-dupes, kept both, treated `_DEMO` as canonical); the demo *code* lives in the `neuralith` repo not here. No `_legacy/`. Subagent flagged stage="scoping" not "pre-scoping/intro" — disagree if you think otherwise.
   - **agrolimen** (operating co = **GB Foods Europe**; sister co Affinity Pet Care not in scope) — `axion-extraction`, `contracting`. Turn GB Foods' scattered raw-material spec PDFs (3 heterogeneous types — internal "new format", legacy internal "old format" often Spanish, external supplier sheets EN/IT/ES/FR/DE) into a PLM-ready relational spec DB; hard part = LLM matching supplier specs to the right internal master across processing states (fresh/frozen/chopped/dehydrated/powder). Project lead **Patricia Pastoriza** (Agrolimen Digital Transformation); Quality SMEs **Núria De Francisco Camprubi** + **Lidia**; **Marco Sterbizzi** earlier AI-governance contact. 1wk scoping pilot late-March (47 PDFs through the 4-step pipeline: Chandra OCR → GPT-5.x classify+extract → LLM match → 14-table relational DB + interactive HTML), presented Apr 7. **PoC SoW drafted not signed** (10wk, 100 docs, €50k upfront, 15-table schema) — this is the "changed SoW" (pilot was ~50 docs/14-table). A *second* prospective scope surfaced Apr 24 / May 8: an "intelligent vendor onboarding" MVP (extraction + risk screening + app, ~€50k, phase-gate pricing being floated) — being scoped in parallel. Client go/no-go pending, paced partly by a key stakeholder's wedding (~3wk out). **Open:** schema 14 vs 15 tables (client hands over the definitive one at kickoff); the Dec-2025 intro + Dec-2025 technical-deep-dive + Jan-2026 ~1.7hr technical call are referenced but have no transcripts on file; whether the vendor-onboarding MVP bundles with / follows / runs independent of the spec-extraction PoC; NDA signed (verbally confirmed Mar-27, IRIS signatory Victor Botev) but signed copy not in folder. The `2026-02-10` transcript is filename-labelled "Patricia" but is actually Marco (Tactiq mislabel).
   - **garrett** (= **Garrett Motion**, Nasdaq GTX — world's largest turbocharger maker, spun out of Honeywell 2018) — `axion-extraction`, `scoping`. Free at-IRIS-cost capability showcase: contact **Liviu Nitu** (AI Innovation Lead, technically deep) evaluating whether IRIS's doc-understanding pipeline (Chandra 2 OCR + DocLayout-YOLO + GPT-4o vision + GPT-4.1 translate/analysis) does something meaningfully better than Garrett's *internal* pipeline (already does narrow OEM-spec→component matching at ~97.1-97.2%). Deliverable = self-contained interactive HTML showcase on **public** Garrett material (144-page Performance Catalog 2025 + 3 Garrett/Honeywell patents FR/DE/ES) — no NDA. Iterated v1→v2→v3; current = `garrett_showcase_v3.html` (~20MB, built Apr 28). Pitch = "complementary depth, not beating your 97% matcher". **Open / thin:** only ONE meeting record exists (Feb 6 HubSpot intro note) — the Feb 13 CPO follow-up happened but no transcript; no scoping-stage call records (iteration ran async/Slack), so the timeline from late Feb is reconstructed from mtimes; whether v3 has actually been shown to Liviu and the response is unrecorded — last dated movement Apr 28. Gated on Liviu finding declassifiable Garrett docs for a real benchmark (needs NDA + Garrett-internal declassification). Note: "Vankata"/"Vankita" appears as *Garrett's CPO* AND as IRIS's Ivan Tsenov — two different people.
   - **welocalize** (= **Welo Life Sciences**) — `axion-extraction`, `scoping`. OCR + structure-aware extraction + translation of multilingual **Bayer pharmacovigilance** forms. Sample = 7 scanned adverse-event/biovigilance forms in 5 languages (DE×2, FR, KO, TH×2, ZH bilingual), 9 pages. Pipeline emits HTML/MD/TXT/JSON/DOCX per page+doc preserving form structure / checkbox states / strike-throughs / source-correction normalisation, with English tooltips on non-English text. Deliverable = single self-contained ~20MB HTML walkthrough + a 20-25min talking script. Walkthrough to Welo's **Eirini Kouvatsi** + **Yufan Yang** not yet held. **Open / thin:** *no transcripts at all* — no `notes/` folder; the intro/scoping-kickoff conversation + the Welo walkthrough call are not in the migrated material; STATUS dates (Apr 21/29/30) reconstructed from mtimes only — needs backfill. No commercial detail on file (no HubSpot link, talking-script pricing is a placeholder "€X-Y / 100 docs / 8-12wk", no NDA/SoW status). Two scoping runs (Scoping 1 = Apr 21, Scoping 2 = Apr 29); demo built from Scoping 2.
   - **tandf** (= **Taylor & Francis**, academic publisher, part of Informa) — `axion-extraction`, `scoping`. Their AI Applied Solutions group (**Paolo Lombardi** director; **David George** CS PhD) building a "Knowledge Architect": extract `practice → predicate → outcome` triples from scientific articles, ground to ontologies, aggregate corroborating/conflicting findings into a knowledge graph, feed a RAG chatbot. T&F builds this internally too and benchmarks vendors against its own build. Inbound via Jordan; first call Apr 15. Free 50-article extraction run (25 pharma + 25 agri/food journals, both publisher PDF and JATS XML) delivered as a JSON bundle + HTML viewer at the **scoping presentation 2026-05-12** (Ivo + Jordan + Ivan Tsenov / Paolo + David). Output: ~8,200 grounded triples (4,491 XML / 3,718 PDF), 4,152 RankedClaims, schema extensions over Paolo's data model. Next: IRIS sends bundle + viewer + recording + NDA template + formal proposal; T&F does ~4wk internal stakeholder review + budget verification → go/no-go on a paid pilot (~4-10wk, at cost ~€50k with IRIS absorbing ~€25k, on T&F's AWS VPC). No NDA yet. **Open:** no HubSpot link; pilot pricing is Jordan's verbal guess; the exact T&F predicate ontologies aren't pinned (David pasted SNOMED-CT / MeSH / NCIt as "starting points, hundreds more"); NDA signatory (Jordan vs Viktor) not decided; T&F's CTO (the budget sponsor) isn't named in any migrated file. Scoping-presentation transcript put in `2_scoping/transcripts/`, the Apr 15/29 + emails in `1_pre-scoping/transcripts/`.

7. **2026-05-13 — batch 2 migrations + MCPs wired + Drive-Gemini digest run.** Several things this day:
   - **3 more engagements migrated** (`engagements/` commit `2de190f`): **deutschebahn** (= DB InfraGO AG; `axion-extraction`, `scoping` — "Digitizing Local Traffic Control Documents": extract structured data from German rail ops docs — Betriebsstellenbuch / Streckenbuch / ESTW — into a DB for InfraGO's dispatching algorithm; champion **Hannah Richta**, Head of Algorithms; ~300 docs, one-time extraction; **scoping-results review Fri 15 May 2026 14:00** with Hannah + Jordan + Vankata; commercial = scoping free → ~€50k MVP w/ quality gate → ~€100k full impl, DB procurement slow → kickoff ~Aug; folder is thin — 2 transcripts, no extractor code or scoping deliverable found in Axion). **fresenius** (= Fresenius Medical Care; `axion-extraction`, `scoping` — **lingering, last activity 2026-01-23**: medical-research-literature extraction into `.iris.json` chunks; champion **Gerome Newport Fischer** + SME **Andreas Maierhofer**; 25-paper nephrology sample; the v4 deliverable + cost-per-doc may never have been shipped — revive-or-close decision). **nhs** (= Dr **Mazhar Ajaz**, NHS oncologist hosting BNOS 2027; `TBD`, `intro` — a collaborative hypothesis-generation platform for neuro-oncology researchers over ~100K PubMed glioblastoma papers; one Jordan call 2026-03-27; "NHS" is a placeholder, the realistic buyer is a brain-tumour charity grant; budget "a few K" vs PoC floor €25K).
   - **daikin bootstrapped** (`engagements/` commit `eb97bae`) — Daikin Europe N.V. (HVAC); new inbound lead via Jordan; **intro call was 2026-05-13 12:00-12:45** (Jordan + Haruka Degawa + Maria Volkova + Ivo); HubSpot booking note: "Data extraction and structuring"; folder is mostly placeholders — fill from the call transcript. Manual version of the Calendar-trigger "new engagement" path.
   - **finom bootstrapped** (committed with this batch) — Finom (EU fintech/business-banking for SMEs); inbound via Jordan; **intro call 2026-05-14 14:00-14:45** (Jordan + Valeriy Dmitriev + Veronika Koleva + Ivo); booking note: "we already have several RAG implementations... happy to hear about your solutions" → comparison play like Garrett/Postbank, lead with what's *different*. `intro`/`rag(TBD)`, mostly placeholders.
   - **Google Calendar + Gmail + Drive MCPs are now wired** (the claude.ai connectors, via `/mcp`). Calendar reads fine (read-only use: morning brief / meeting-prep matching). **Discovery: most meetings have a "Notes by Gemini" Google Doc in Drive** (titled `<meeting> - <date time tz> - Notes by Gemini`; some also have `... - Transcript` docs) — these are NOT attached to the calendar event, they live in Drive; the engagement folders were stale because this content was never downloaded to `Axion/scoping/`. **There is no Tactiq MCP** — only the manual `Axion/tactiq_export/export.py` (hardcoded token); treat Tactiq as legacy, lean on Drive Gemini-notes + Calendar instead. HubSpot meeting-booking notes are embedded in Jordan's calendar events (`Company name:` + `What would you like to get from this meeting:`) — a free lead-source/intent signal usable via Calendar alone.
   - **Drive-Gemini-notes digest run** (committed with this batch) — ran the LOOPS.md #2 "post-meeting digest" retroactively on 6 engagements via parallel subagents (`atlas/_digest_protocol.md` is the reusable procedure): folded fresher meeting content into `STATUS/CONTEXT/SOURCES/COMMERCIAL`. Key updates:
     - **garrett** — the showcase WAS presented to Liviu on **7 May 2026** (the migration's big open question, now closed): Liviu's reaction **positive** ("increased their trust", "quite solid", "gives a bit of hope we can work onto your second stage"); moved straight to scoping a **paid co-funded MVP/pilot (€25-75k**, ~€50k midpoint, 4-10wk, ~€10k AWS credits each side); use case **sharpened** — Garrett wants IRIS for **ingestion + data segregation + data labeling** of the unstructured multilingual incoming OEM specs (Mercedes/Renault/GM/Ferrari), NOT the downstream matching (they build that); Baala's headline ask = high-accuracy extraction of "design standard documents" (text + 2D callout drawing, context-only, no fine-tuning). New people: **Baala Sangar B.S.** (`b.sbaalasangar.sankaramoorthy@garrettmotion.com`, leads Garrett's AI app dev — owns the project; Liviu now "overseeing/consulting"), **Alex** (Garrett Head of AI — the actual sign-off), **Diana Zot** (Garrett's AWS account owner). NDA gate moved (Liviu commits the MVP "would streamline us into getting some NDAs"). **The "Garrett CPO Vankata" was a migration error — corrected; "Vankata" = Ivan Tsenov, IRIS CPO, in all transcripts.** Next: follow-up call **19 May 2026 13:30** (Liviu's internal answer after briefing Alex/Legal/Cyber/AWS).
     - **postbank** — the **28 Apr Sofia F2F happened** (both Svetla and Viktor reference it on 8 May); engagement **warming**: scope **narrowed to retail-banking campaign analysis** on a handful of SFMC Data Extensions (Svetla's "min work, max visible result"; maybe purpose-built DEs not production ones), price **~€40k with IRIS co-funding** (Postbank pilots run <€50k; Viktor signed off in principle, contingent on a clear expansion path); **still pre-NDA** (Jordan offered an MNDA 8 May, Svetla won't commit — has to ask **Lena** (= "Lina", ≈ VP Marketing & Sales Ops, first line of budget approval); a Postbank NDA can take longer than the pilot, may fold into the contract), still synthetic-data-only; now gated on Postbank's committees (first committee = make-or-break → architecture/IT/infosec → GDPR + EU-AI-Act → second committee → legal → work starts, "several months"). Solid AI not mentioned 8 May. Success bar Svetla set = "can it do this, and how fast" (latency parity with their multi-minute SFMC queries), NOT a %-accuracy figure. Next: scope call **14 May 2026 11:00** (Jordan to send indicative pricing first).
     - **agrolimen** — now explicitly **two parallel workstreams**: (1) the spec-extraction PoC / "Digitization MVP" (contracting; scope narrowed 24 Apr to **internal specs only**; production universe ~4,000-6,000 internal raw-material specs, *not packaging*; the old "~10,000" was internal+supplier), and (2) a **vendor-onboarding MVP** being scoped (~€40-45k effective rate after stripping the ~€75k "supplier portal" UI from the ~€100k at-cost, IRIS co-funds ~€10k more, +€5-10k AWS credits; **success-based phase-gate** payment — half upfront, half at a mid-MVP quality checkpoint, fail→stop; ~14wk). The two ship as **separate SoWs, stage-gated** (workstream 2 needs the internal-spec DB from workstream 1) — though Patricia wants a single combined proposal for procurement (Jordan agreed, warned it slows procurement). New facts: GB Foods mid an SAP→S/4HANA migration with a change freeze (IRIS offered its AWS-partnered migration team as a cross-sell); GB Foods rolling out Ariba for vendor onboarding (~spring 2027 — future API target); internal go/no-go may slip to end-May/June (stakeholder's ~3wk wedding leave). Stakeholder fixes: **Lidia Vargas** (`lvargas@thegbfoods.com`); `omesia@ag-group.com` on most invites (role TBC). Org label is "AG Connectivity Hub (GB Foods + Affinity Petcare)" — clarify whether vendor-onboarding spans Affinity too. Schema 14-vs-15: not a real disagreement (15 = the 14 with Allergen + Chemical-Contaminant master lists split out). Next: vendor-onboarding scoping call **14 May 2026 16:00**.
     - **sms-group** — Gerald demo still **tentatively ~19-22 May, NOT booked** (Thorsten owns it; ~2h, IRIS CTO+CRO+CPO+Jordan; 12 May internal demo-planning call settled the plan but not the date); **Matthias = General Manager** (the senior SMS business stakeholder under Gerald, and the continuity owner once Gerald retires autumn 2026 — resolves the "identify a business stakeholder" open Q). Function-text column **IS being extracted from the EP1 PDF**. SMS is running the same PoC with other vendors (be humble about the multi-million valuation in the proposal). New people: Yevhenii Kondratiev (designer polishing the demo UI), Steven Fung (CRO, co-owns the proposal), Victor (CTO, not needed in the final SMS presentation). Note: the "PoC Wrap Up & Commercial Next Steps - 2026/04/30" Drive doc is an **internal IRIS portfolio meeting** (SMS + Aumovio + Heineken + "Continental" + Agrolimen), not an SMS call.
     - **aumovio** — **contract figures pinned** (from IRIS's Sept-2025 proposal + the signed Commercial Proposal): **PoC €17,600 (€15,000 ODL + €2,600 ingestion)**, **full implementation €65,000** (5,000-doc package, 50% discount, €0.65/page — the "~€60k Norbert rounded"), **+€7,500 ODL-change add-on** → ~€90k total. The Commercial Proposal is a Google Doc on Drive (Victor Botev signs as CEO); the PO doc itself still not on file (only the €17,600 PoC was PO'd). **Important: the numeric quality metrics** the team frets about (priority-field F1 ≥0.92 etc.) are NOT in the signed SoW — they're in **IRIS's own Sept-2025 proposal §8** ("steady-state targets, to be finalised with client"). Norbert = budget owner + rollout project lead; the ultimate business/economic decision-maker (the eval-deliver sign-off) is still unnamed ("someone from sales"). Software Impact Analysis adjacent opportunity (R&D code-change impact, ~1,800 engineers, ISO 26262) — tracked separately. Next: "At Scale Extraction Sync" with Norbert + Phil **today 2026-05-13 12:45**.
     - **tandf** — no new substance: the folder's `.txt` transcripts were already more complete than the Gemini notes (which truncate). Confirmed David George's email; the predicate ontologies are still just SNOMED-CT/MeSH/NCIt "starting points"; NDA signatory + T&F CTO still unnamed. There's a Drive `TandF/` working folder + the 12 May call recording (an item IRIS owes Paolo).
   - **A "Continental" engagement surfaced with no folder** — mentioned in the Apr 30 internal portfolio call: champion **Patricia** (different Patricia from agrolimen's? or same? — unclear), revoked their PoC scope, want a new UI piece, ~$100k combined proposal, **Blinkin** as UI subcontractor. Distinct from Aumovio (which IS the Continental Safety & Motion carve-out). Needs a folder + clarification.

   **Calendar — other prospective engagements seen** (Jordan pipeline, no folders, mostly intro calls): **Finom** (folder now bootstrapped), **Basamh** (Mohammed / Jordan, 27 Apr — Saudi conglomerate), **Jordan Ahli Bank** (Omar / Jordan, 14 Apr), **SPIE** (Serge / Jordan, 30 Apr + 7 May), **INDEVCO Group** (placeholder, 23 Apr — Lebanese packaging group), **Mohamed / Jordan** (23 Apr — possibly same as Basamh or Ahli), **HubSpot themselves** (Chris / Jordan — 3 catch-ups; HubSpot is a customer/partner relationship, not a delivery engagement). Plus **Continental** (above). Bootstrap these into `engagements/<name>/` (or `_pending/`) as they become real — the Calendar/Drive-Gemini-notes give you most of what's needed.

   **Engagements NOT migrated yet** (still in `Axion/scoping/`): the user's status (2026-05-13): **alexfert** = was pre-PoC, halted (war in Iran); **shanghai-synocodes** = halted; **aumovio-soft-impact** = halted; **stepan** = declined; **hubspot** (as a delivery engagement) = declined (competitor); **riyadbank** = initial call only; **fresenius** = lingering (now migrated, see above); **nhs** = had a call (now migrated); **deutschebahn** = demo Friday (now migrated); **ahli / aramco / allianz / basamh / brunela / cetin / ciklum / lifescience / orion / yettel** = unclear / don't remember / Jordan-Steven's / "idk what it is" (yettel is actually the RAG *reference architecture* — "same as the Yettel RAG" appears in the Heineken docs — probably closed-won, prior). So: no third migration batch needed beyond what's done unless one of the "unclear" ones reactivates; the halted/declined ones could get one-line tombstone `STATUS.md`s (low priority).

## What's next

**Ivo reviews the migrated folders** — Heineken (the proof), then spot-check a couple of the batch-A4/batch-2 ones — and answers the acceptance question per engagement: **"could a tech person start this PoC/scoping cold from `CONTEXT/STATUS/SOURCES/COMMERCIAL`, without asking me anything?"** If the template's holding up → start Track B. If a section is consistently thin → fix `_template/` first, then the migrated folders are cheap to re-do (the `AXION:` sources are untouched).

**Pending top-ups Ivo flagged:** he'll provide fresher notes/transcripts for the already-migrated engagements (the picture is still a bit stale where he hasn't downloaded newer-meeting transcripts) — when he does, run the digest again. Also: drop the Daikin (13 May) and Finom (14 May) intro-call transcripts/Gemini-notes in once they exist and fill those folders. And: create a `Continental` folder + clarify the "two Patricias" question.

**Loops — first one is in reach now the Calendar MCP is wired:** LOOPS.md #1 (daily morning brief) and #2 (post-meeting digest) are both buildable. The digest ran retroactively today as a one-off (parallel subagents off `_digest_protocol.md`); the natural next step is to run it on-demand after each call (drop transcript / point at the Drive Gemini-notes doc → fold into STATUS). The morning brief: each morning, list today's client calls, match to `engagements/<company>/`, pull CONTEXT + recent STATUS → a one-pager. The matching keys work well in practice (attendee email domains + the `Aliases:` line; Jordan's HubSpot-booked events even carry `Company name:`).

Then, roughly: B1 (S3 prefix standardisation) ‖ B2 (reusable scoping harness, the biggest hours-saver after the context system — several engagements have bespoke extractor code in `2_scoping/extraction/` or `4_poc/eval/` that B2 would absorb: agrolimen's 4-step pipeline, tandf's ~25-py extractor, garrett's Chandra/YOLO scripts, aumovio's 3-step pipeline, sms-group's table-extractor, fresenius's pipeline, welocalize's build.py). B3 (EC2 worker), B6 (PoC status dashboard — falls out cheaply now the folders exist; rows = these ~13 engagements, cols = weeks, cells coloured by stage; this week's calendar showed how dense the checkpoint schedule is — Postbank Thu, Agrolimen Thu, DB InfraGO Fri, Heineken Mon, Garrett Tue 19, SMS demo ~19+). See `ENGAGEMENT_PLAN.md` §6 for the full interleave.

## Still open / things Ivo needs to find out

- Who has **HubSpot admin** (to authorize a read-only HubSpot MCP). Calendar + Gmail + Drive MCPs are now wired; HubSpot is the remaining one. Note: Jordan's HubSpot-booked calendar events already leak `Company name:` + `What would you like to get from this meeting:` — partial substitute until the real MCP.
- How Jordan **names calendar events** — observed (this week's pull): client calls he organises are `<Client contact first name> (<Company>) / Jordan (Iris.ai)` with the company + intent in the event body; internal/PoC syncs are `<Company> <> Iris.ai -- <topic>` or `<topic> Sync`. Matching by attendee email domain + `Aliases:` + the `Company name:` body field works well. Defensive fallback still needed for the `<topic> Sync`-style ones (no company in the title).
- **EC2 box specifics** — Chandra is GPU-ish, so `t3.small` is likely not enough (`g4dn`/`g5`, or call a hosted OCR endpoint à la AlexFert's PaddleOCR — note Garrett's Chandra-2 run used a `g5.2xlarge` in eu-central-1, ad-hoc); region; auth via instance profile + SSM Parameter Store.
- **S3 bucket** — confirm with Ross what he provisioned and whether it's the same bucket the Neuralith upload link writes to.
- **Google Drive** — it's now wired (MCP). It's also where the Gemini meeting-notes live (a discovered read-source). Decide: keep as a one-way git→Drive projection for the tech team, or just give them repo / dashboard-link access. Lower priority now that Drive is reachable read-only.
- **Tactiq** — no MCP. Treat as legacy; the only path is `Axion/tactiq_export/export.py` (hardcoded token — move to a secrets store if it's kept). Drive Gemini-notes + Calendar cover most of what Tactiq gave.
- **The "two Patricias"** — agrolimen's project lead is Patricia Pastoriza (`ppastoriza@ag-group.com`); the "Continental" engagement in the Apr 30 internal call also has a champion called "Patricia" who revoked their PoC scope. Same person, two engagements? Or different? Clarify.
- **`Axion/` cleanup** — separate exercise; sort `*-usecase/` (reference), `tactiq_export/` (tooling — move the hardcoded tokens to a secrets store), playground demos, loose files, the un-migrated `scoping/*` dirs (most are halted/declined/unclear — see batch-2 notes). Not blocking.

## Memory note

This work also has an auto-memory entry under `~/.claude/projects/-Users-iris-Documents-Axion/memory/`
pointing here. A fresh Claude Code session started in `~/Documents/atlas/` or `~/Documents/engagements/`
gets a *different* (empty) auto-memory folder — so the durable memory is **this file + `ENGAGEMENT_PLAN.md`**.
First thing a new session should do here: read `STATE.md` then `ENGAGEMENT_PLAN.md`.
